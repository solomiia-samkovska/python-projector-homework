# Simple CLI Calculator


## What is it?
This is a simple command-line interface (CLI) calculator application that allows you to perform basic arithmetic operations.

## Installation
No installation is required. Just download the `app.py` file to your local machine.

## Main Features
The calculator supports the following operations:

- **Addition:**
- **Subtraction:**
- **Multiplication:**
- **Division:**

## Usage
1. Run the `app.py` file using the Python interpreter.
2. Follow the on-screen instructions to perform arithmetic operations.

## Note
 Division by zero is not allowed and you will get an error message.

